/**
 * This code was generated by "react-native codegen-lib-harmony"
 */

import {
  Descriptor as ComponentDescriptor,
  ViewBaseProps,
  ViewRawProps,
  ViewDescriptorWrapperBase,
  ColorValue,
  Color,
  RNInstance,
  Tag,
  RNComponentCommandReceiver,
  ViewPropsSelector,
} from '@rnoh/react-native-openharmony/ts';


export namespace RNCSlider {
  export const NAME = "RNCSlider" as const

  export interface DirectRawProps {
    accessibilityUnits?: string;
    accessibilityIncrements?: string[];
    disabled?: boolean;
    inverted?: boolean;
    vertical?: boolean;
    tapToSeek?: boolean;
    maximumTrackImage?: string;
    maximumTrackTintColor?: ColorValue;
    maximumValue?: number;
    minimumTrackImage?: string;
    minimumTrackTintColor?: ColorValue;
    minimumValue?: number;
    step?: number;
    testID?: string;
    thumbImage?: string;
    thumbTintColor?: ColorValue;
    trackImage?: string;
    value?: number;
    lowerLimit?: number;
    upperLimit?: number;
  }
  
  export interface Props extends ViewBaseProps {}
  
  export interface State {}
  
  export interface RawProps extends ViewRawProps, DirectRawProps {}
  
  export class PropsSelector extends ViewPropsSelector<Props, RawProps> {
    get accessibilityUnits() {
      return this.rawProps.accessibilityUnits;
    }
    
    get accessibilityIncrements() {
      return this.rawProps.accessibilityIncrements;
    }
    
    get disabled() {
      return this.rawProps.disabled ?? false;
    }
    
    get inverted() {
      return this.rawProps.inverted ?? false;
    }
    
    get vertical() {
      return this.rawProps.vertical ?? false;
    }
    
    get tapToSeek() {
      return this.rawProps.tapToSeek ?? false;
    }
    
    get maximumTrackImage() {
      return this.rawProps.maximumTrackImage;
    }
    
    get maximumValue() {
      return this.rawProps.maximumValue ?? 0;
    }
    
    get minimumTrackImage() {
      return this.rawProps.minimumTrackImage;
    }
    
    get minimumValue() {
      return this.rawProps.minimumValue ?? 0;
    }
    
    get step() {
      return this.rawProps.step ?? 0;
    }
    
    get testID() {
      return this.rawProps.testID;
    }
    
    get thumbImage() {
      return this.rawProps.thumbImage;
    }
    
    get trackImage() {
      return this.rawProps.trackImage;
    }
    
    get value() {
      return this.rawProps.value ?? 0;
    }
    
    get lowerLimit() {
      return this.rawProps.lowerLimit ?? 0;
    }
    
    get upperLimit() {
      return this.rawProps.upperLimit ?? 0;
    }
    
  
    get maximumTrackTintColor() {
        if (this.rawProps.maximumTrackTintColor) {
          return Color.fromColorValue(this.rawProps.maximumTrackTintColor)
        } else {
          return new Color({ r: 0, g: 0, b: 0, a: 255})
        }
    }
    
    get minimumTrackTintColor() {
        if (this.rawProps.minimumTrackTintColor) {
          return Color.fromColorValue(this.rawProps.minimumTrackTintColor)
        } else {
          return new Color({ r: 0, g: 0, b: 0, a: 255})
        }
    }
    
    get thumbTintColor() {
        if (this.rawProps.thumbTintColor) {
          return Color.fromColorValue(this.rawProps.thumbTintColor)
        } else {
          return new Color({ r: 0, g: 0, b: 0, a: 255})
        }
    }
    
  }

  export type Descriptor = ComponentDescriptor<
    typeof NAME,
    Props,
    State,
    RawProps
  >;
  
  export class DescriptorWrapper extends ViewDescriptorWrapperBase<
    typeof NAME,
    Props,
    State,
    RawProps,
    PropsSelector
  > {
    protected createPropsSelector() {
      return new PropsSelector(this.descriptor.props, this.descriptor.rawProps)
    }
  }
  
  export interface EventPayloadByName {
    "change": {value: number, fromUser?: boolean}
    "rNCSliderSlidingStart": {value: number, fromUser?: boolean}
    "rNCSliderSlidingComplete": {value: number, fromUser?: boolean}
    "rNCSliderValueChange": {value: number, fromUser?: boolean}
  }
  
  export class EventEmitter {
    constructor(private rnInstance: RNInstance, private tag: Tag) {}
    
    emit<TEventName extends keyof EventPayloadByName>(eventName: TEventName, payload: EventPayloadByName[TEventName]) {
      this.rnInstance.emitComponentEvent(this.tag, eventName, payload)
    }
  }
  
  export interface CommandArgvByName {
  }
  
  export class CommandReceiver {
    private listenersByCommandName = new Map<string, Set<(...args: any[]) => void>>()
    private cleanUp: (() => void) | undefined = undefined
  
    constructor(private componentCommandReceiver: RNComponentCommandReceiver, private tag: Tag) {
    }
  
    subscribe<TCommandName extends keyof CommandArgvByName>(commandName: TCommandName, listener: (argv: CommandArgvByName[TCommandName]) => void) {
      if (!this.listenersByCommandName.has(commandName)) {
        this.listenersByCommandName.set(commandName, new Set())
      }
      this.listenersByCommandName.get(commandName)!.add(listener)
      const hasRegisteredCommandReceiver = !!this.cleanUp
      if (!hasRegisteredCommandReceiver) {
        this.cleanUp = this.componentCommandReceiver.registerCommandCallback(this.tag, (commandName: string, argv: any[]) => {
          if (this.listenersByCommandName.has(commandName)) {
            const listeners = this.listenersByCommandName.get(commandName)!
            listeners.forEach(listener => {
              listener(argv)
            })
          }
        })
      }
  
      return () => {
        this.listenersByCommandName.get(commandName)?.delete(listener)
        if (this.listenersByCommandName.get(commandName)?.size ?? 0 === 0) {
          this.listenersByCommandName.delete(commandName)
        }
        if (this.listenersByCommandName.size === 0) {
          this.cleanUp?.()
        }
      }
    }
  }

}
